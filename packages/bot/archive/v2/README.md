# Bot AI v2 (archived)

This folder contains the previous bot decision stack: task selection, path planning,
TaskNavigator, stuck recovery, skills, and integration tests.

It was archived so NPC practice robots can spawn as physical bodies on the field
without any autonomous drive or mechanism commands while the brain is redesigned.

To restore: move modules back under `packages/bot/src/` and re-wire `BotManager`.
